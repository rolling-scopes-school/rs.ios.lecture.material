
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

// MARK: - Definitions

typedef struct {
    char *name;
    size_t age;
} person_t;

// MARK: - Prototypes

void login_user(person_t *user);

// MARK: - Implementations

int main(int argc, char * argv[]) {
    return 0;
}